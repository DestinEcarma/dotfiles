####################
# Global Variables #
####################

# Theme
$THEME = Orchis-Dark
$ICONS = Tela-dracula-dark
$COLOR = prefer-dark

# Cursor
$CURSOR_THEME = Bibata-Modern-Ice
$CURSOR_SIZE = 32

# Monitor
$SCALE = 1.2
$MAIN_MONITOR = ,preferred,auto,$SCALE

$MOUSE_SENSITIVITY = -0.25

# Sizing
$GAPS_IN = 3.5
$GAPS_OUT = 7
$ROUNDING = 5

# Applications
$TERMINAL = kitty
$FILE_MANAGER = nautilus
$MENU = rofi -show drun -theme ~/.config/rofi/themes/drun.rasi
$CALCULATOR = rofi -show calc -modi calc -no-show-match -no-sort -theme ~/.config/rofi/themes/calc.rasi
$WALLPAPER_MANAGER = waypaper
$SCREENSHOT_WINDOW = hyprshot -m window
$SCREENSHOT_REGION = hyprshot -m region
$SCREENSHOT_FULLSCREEN = hyprshot -m output
$COLOR_PICKER = hyprpicker -a

# Main modifier key
$MOD = SUPER

#################
# Custom Config #
#################

source = ./hyprland-init.conf

############
# Monitors #
############

# Main monitor
monitor = $MAIN_MONITOR

##############
# Auto Start #
##############

source = /home/destin/.cache/wal/colors-hyprland.conf

exec-once = swww-daemon
exec-once = swaync
exec-once = swayosd-server
exec-once = hyprpaper
exec-once = waybar 
exec-once = $TERMINAL
exec-once = exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

###########################
# Environmental Variables #
###########################

# Default graphics card to use.    |- Fallback to a different card for some unkown reason that the first card does not exist.
env = AQ_DRM_DEVICES,/dev/dri/card1:/dev/dri/card0

# Scale factor for GDK applications
env = GDK_SCALE,$SCALE

# Theme
env = GTK_THEME,$THEME
env = ICON_THEME,$ICONS
env = COLOR_SCHEME,$COLOR

# Cursor
env = HYPRCURSOR_THEME,$CURSOR_THEME
env = HYPRCURSOR_SIZE,$CURSOR_SIZE
env = XCURSOR_THEME,$CURSOR_THEME
env = XCURSOR_SIZE,$CURSOR_SIZE

###############
# Layer Rules #
###############

# Waybar
layerrule = blur,waybar|swaync-control-center|swaync-notification-window
layerrule = ignorezero,waybar|swaync-control-center|swaync-notification-window
layerrule = ignorealpha 0.5,swaync-control-center|swaync-notification-window

# Wlogout
layerrule = blur,logout_dialog

################
# Window Rules #
################

windowrule = float,class:float
windowrule = float,class:zen,title:Library
windowrule = float,class:(org.gnome.Nautilus|org.gnome.Loupe)
windowrule = float,title:(File Upload|Open Image|Select Recording Directory)

#################
# Look And Feel #
#################

# Unscale XWayland
xwayland {
	force_zero_scaling = true
}

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = $GAPS_IN
    gaps_out = $GAPS_OUT

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = $color2
    col.inactive_border = $background

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = $ROUNDING

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 4
        passes = 2

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#########
# Input #
#########

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = $MOUSE_SENSITIVITY # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


############
# Keybinds #
############

# See https://wiki.hyprland.org/Configuring/Keywords/

# Fullscreen
bind = $MOD SHIFT, F, fullscreen

# Wallpaper
bind = $MOD SHIFT, W, exec, $WALLPAPER_MANAGER

# Screenshot
bind = $MOD, S, exec, $SCREENSHOT_WINDOW
bind = $MOD SHIFT, S, exec, $SCREENSHOT_REGION = hyprshot -m region
bind = $MOD CTRL, S, exec, $SCREENSHOT_FULLSCREEN

# Color Picker
bind = $MOD SHIFT, C, exec, $COLOR_PICKER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $MOD, RETURN, exec, $TERMINAL
bind = $MOD, Q, killactive,
bind = $MOD, M, exit,
bind = $MOD, E, exec, $FILE_MANAGER
bind = $MOD, V, togglefloating,
bind = $MOD, SPACE, exec, $MENU
bind = $MOD, C, exec, $CALCULATOR
bind = $MOD, P, pseudo, # dwindle
bind = $MOD, T, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $MOD, H, movefocus, l
bind = $MOD, J, movefocus, d
bind = $MOD, K, movefocus, u
bind = $MOD, L, movefocus, r

# Switch workspaces with mainMod + [0-9]
bind = $MOD, 1, workspace, 1
bind = $MOD, 2, workspace, 2
bind = $MOD, 3, workspace, 3
bind = $MOD, 4, workspace, 4
bind = $MOD, 5, workspace, 5
bind = $MOD, 6, workspace, 6
bind = $MOD, 7, workspace, 7
bind = $MOD, 8, workspace, 8
bind = $MOD, 9, workspace, 9
bind = $MOD, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $MOD SHIFT, 1, movetoworkspace, 1
bind = $MOD SHIFT, 2, movetoworkspace, 2
bind = $MOD SHIFT, 3, movetoworkspace, 3
bind = $MOD SHIFT, 4, movetoworkspace, 4
bind = $MOD SHIFT, 5, movetoworkspace, 5
bind = $MOD SHIFT, 6, movetoworkspace, 6
bind = $MOD SHIFT, 7, movetoworkspace, 7
bind = $MOD SHIFT, 8, movetoworkspace, 8
bind = $MOD SHIFT, 9, movetoworkspace, 9
bind = $MOD SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
# bind = $MOD, S, togglespecialworkspace, magic
# bind = $MOD SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $MOD, mouse_down, workspace, e+1
bind = $MOD, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $MOD, mouse:272, movewindow
bindm = $MOD SHIFT, mouse:272, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, swayosd-client --output-volume raise
bindel = ,XF86AudioLowerVolume, exec, swayosd-client --output-volume lower
bindel = ,XF86AudioMute, exec, swayosd-client --output-volume mute-toggle
bindel = ,XF86AudioMicMute, exec, swayosd-client --input-volume mute-toggle
bindel = ,XF86AudioNext, exec, swayosd-clientjj
bindel = ,XF86MonBrightnessUp, exec, swayosd-client --brightness raise
bindel = ,XF86MonBrightnessDown, exec, swayosd-client --brightness lower

# Requires playerctl
bindl = , XF86AudioNext, exec, swayosd-client --playerctl next
bindl = , XF86AudioPause, exec, swayosd-client --playerctl play-pause
bindl = , XF86AudioPlay, exec, swayosd-client --playerctl play-pause
bindl = , XF86AudioPrev, exec, swayosd-client --playerctl previous


##########################
# Windows And Workspaces #
##########################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
